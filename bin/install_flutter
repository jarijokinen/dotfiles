#!/bin/bash

wget -O /tmp/flutter.tar.xz https://storage.googleapis.com/flutter_infra/releases/stable/linux/flutter_linux_v1.9.1+hotfix.2-stable.tar.xz

mkdir -p $HOME/opt
tar xf /tmp/flutter.tar.xz -C $HOME/opt

cat <<-'EOF' >> $HOME/.bashrc
export PATH=$PATH:$HOME/opt/flutter/bin
EOF

exit 0
